import OnboardForm from './OnboardForm';

export const metadata = {
  title: 'Onboard Clinic — NexaCare Admin',
  robots: 'noindex, nofollow',
};

export default function OnboardClinicPage() {
  return <OnboardForm />;
}
